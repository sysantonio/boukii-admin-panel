import { DateUniqueByPipe } from './date-unique-by.pipe';

describe('DateUniqueByPipe', () => {
  it('create an instance', () => {
    const pipe = new DateUniqueByPipe();
    expect(pipe).toBeTruthy();
  });
});
